"""
FastAPI Dependencies — PLACEHOLDER

- get_current_user(token): validates Supabase JWT; raises 401 if invalid
- require_premium(user): raises 403 if free tier

No implementation here. See backend/README.md.
"""
