"""
Stripe Service — PLACEHOLDER

create_checkout_session(user_id, price_id)
create_portal_session(customer_id)
handle_webhook(payload, sig) — subscription lifecycle events

No implementation here. See backend/README.md.
"""
